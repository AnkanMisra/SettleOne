[build]
rootDir = "backend"

[deploy]
startCommand = "env HOST=0.0.0.0 ./bin/settleone-backend"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
